#!/bin/zsh

CONFIG_DIR="$HOME/.config/wezterm/"
PATH="$PATH:$HOME/.local/share/cargo/bin/:$HOME/.local/bin/"
CONFIG="${CONFIG_DIR}/wezterm_modal.lua"
typeset -a cmd=(wezterm --config-file "$CONFIG" ${(z)WEZ_MODAL_CONFIG:-} start --class 'wezmodal' ${@}) 
${(z)cmd[@]}
